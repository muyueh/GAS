/**
 * Simple Apps Script entry point to demonstrate deployment.
 * @return {string} greeting message for testing.
 */
function helloWorld() {
  var message = 'Hello, Google Apps Script!';
  Logger.log(message);
  return message;
}
